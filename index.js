// this will bring over the generate logo function from shapes.js
const { generateLogo } = require('./lib/shapes');

// this will run the generate logo function in the shapes.js folder
generateLogo();